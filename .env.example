# ── Required for production (Vercel) ────────────────────────────────────────
# PostgreSQL connection string. Recommended providers: Neon, Supabase, Railway.
DATABASE_URL=postgresql://user:password@host:5432/dbname

# ── Required for local development only ──────────────────────────────────────
PORT=3000
BASE_PATH=/
